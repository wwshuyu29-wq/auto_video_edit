# Web App

This app is the front desk for the auto video workflow.

## Current Goal

Build a static but realistic control console before wiring real database and cloud storage.

The app reads existing local project JSON artifacts from the repository so the UI reflects real workflow state instead of placeholder demo data.

## Planned Local Run

```bash
cd apps/web
npm install
npm run dev
```

Open `http://localhost:3000`.
