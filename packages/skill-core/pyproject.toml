[project]
name = "auto-video-skill-core"
version = "0.1.0"
description = "Cloud-facing wrapper for the TK video editor skill."
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.0",
]

[tool.setuptools.packages.find]
where = ["src"]
